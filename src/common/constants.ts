export const G = 6.6743e-11; // m^3 / (kg * s^2)
export const EARTH_MASS = 5.972e+24;
export interface Coords {
    x: number,
    y: number,
}
export interface Velocities {
    x: number,
    y: number,
}